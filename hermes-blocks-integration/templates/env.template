# Blocks authentication. `blocks login --write-env --dir <project>` writes the key.
# Never commit this file.
BLOCKS_API_KEY=

# Used by the generated health response and deterministic client.
BLOCKS_AGENT_NAME=<AGENT_NAME>
BLOCKS_TARGET_AGENT=
BLOCKS_TARGET_INPUT_ID=request
BLOCKS_BILLING_MODE=free
BLOCKS_TIMEOUT_MS=130000
BLOCKS_MAX_REQUEST_BYTES=1048576

# Add only capability-specific configuration below.
